pub const SETTINGS_FILE_NAME: &str = "~/.win-containers-registry";

pub const HTTP_PORT: u16 = 8000;

/// Metadata file kept inside every container folder — the only metadata storage
/// of the service.
pub const CONTAINER_INFO_FILE_NAME: &str = "container-info.yaml";
